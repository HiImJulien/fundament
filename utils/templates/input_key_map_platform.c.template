#include "input_key_map_${platform}.h"
#include <fundament/input.h>

${additional_includes}

uint16_t fn__imp_map_${platform}_key(${argument_type} key_code) {
    switch(key_code) {
${switches}
    }
}
